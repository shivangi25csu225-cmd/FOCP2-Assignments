#include <iostream>
using namespace std;

// Recursive function
int digitSum(int n) {
    if(n == 0)
        return 0;
    return (n % 10) + digitSum(n / 10);
}

int main() {
    int n;

    cout << "Enter a number: ";
    cin >> n;

    cout << "Sum of digits = " << digitSum(n);

    return 0;
}
